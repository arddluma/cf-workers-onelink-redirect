{
  "private": true,
  "name": "cf-workers-onelink-redirect",
  "version": "1.0.0",
  "description": "onelink.to alternative on CF workers",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write '**/*.{js,css,json,md}'"
  },
  "author": "arddluma",
  "license": "MIT",
  "devDependencies": {
    "prettier": "^1.18.2"
  }
}
